// Generated by scripts/syncPublicLinks.js. Do not edit directly.
// Canonical public-link contract. Run node scripts/syncPublicLinks.js after edits.
// Self-contained so the same implementation runs in Functions, native and Hosting.
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.PlanLiLinks = factory();
}(typeof globalThis === 'object' ? globalThis : this, function () {
  'use strict';
  const origin = 'https://planli.cc';
  const scheme = 'com.planli.planlitravels://';
  const legacyOrigins = Object.freeze([
    'https://planli-f0b12.web.app', 'https://planli-f0b12.firebaseapp.com',
  ]);
  const paths = Object.freeze({ trip: 'trip', route: 'route', recommendation: 'recommendation' });
  const supportEmail = 'support@planli.cc';
  const stores = Object.freeze({
    ios: 'https://apps.apple.com/il/app/planli-travels/id6801453067',
    android: 'https://play.google.com/store/apps/details?id=com.planli.planlitravels',
  });
  function validId(kind, id) {
    if (typeof id !== 'string' || !Object.hasOwn(paths, kind)) return false;
    if (/[^A-Za-z0-9_-]/.test(id)) return false;
    return kind === 'trip' ? id.length >= 40 && id.length <= 128
      : id.length >= 1 && id.length <= 180;
  }
  function shareUrl(kind, id) {
    if (!validId(kind, id)) throw new Error('Invalid share target.');
    return origin + '/' + paths[kind] + '/' + id;
  }
  function deepLink(kind, id) {
    if (!validId(kind, id)) throw new Error('Invalid share target.');
    return scheme + (kind === 'trip' ? 'shared-trip' : paths[kind]) + '/' + id;
  }
  function parsePath(path) {
    if (typeof path !== 'string' || path.length > 256) return null;
    // No generic URL decoding: reject queries, fragments, extra segments and escapes.
    const match = /^\/?(trip|shared-trip|route|recommendation)\/([A-Za-z0-9_-]+)$/.exec(path);
    if (!match || match[0] !== path) return null;
    const kind = match[1] === 'shared-trip' ? 'trip' : match[1];
    return validId(kind, match[2]) ? { kind, id: match[2] } : null;
  }
  function parseUrl(url) {
    if (typeof url !== 'string' || url.length > 512) return null;
    for (const prefix of [origin + '/', ...legacyOrigins.map(value => value + '/'), scheme]) {
      if (url.startsWith(prefix)) {
        const path = url.slice(prefix.length);
        return path.startsWith('/') ? null : parsePath(path);
      }
    }
    return null;
  }
  return Object.freeze({ origin, scheme, legacyOrigins, paths, supportEmail, stores,
    validId, shareUrl, deepLink, parsePath, parseUrl });
}));
