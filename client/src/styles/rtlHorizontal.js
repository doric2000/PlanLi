import { StyleSheet } from 'react-native';

export const rtlHorizontalStyles = StyleSheet.create({
  nativeContent: {
    flexDirection: 'row-reverse',
  },
  webContent: {
    flexDirection: 'row',
  },
});
