import { StyleSheet } from 'react-native';

import { colors } from './colors';
import { fontFamilies } from './typography';
import { shadows } from './shadows';
import { spacing } from './spacing';

export const recommendationComposerStyles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: colors.appSurface,
  },
  header: {
    backgroundColor: colors.primary,
    paddingHorizontal: spacing.lg,
    paddingTop: spacing.md,
    paddingBottom: spacing.lg,
  },
  headerTopRow: {
    flexDirection: 'row-reverse',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  headerTitle: {
    color: colors.white,
    fontSize: 20,
    fontFamily: fontFamilies.semiBold,
    writingDirection: 'rtl',
  },
  closeButton: {
    width: 44,
    height: 44,
    alignItems: 'center',
    justifyContent: 'center',
  },
  progressCopy: {
    flexDirection: 'row-reverse',
    justifyContent: 'space-between',
    marginTop: spacing.sm,
  },
  progressText: {
    color: '#DCE8F6',
    fontSize: 12,
    writingDirection: 'rtl',
  },
  progressTrack: {
    height: 5,
    borderRadius: spacing.radiusFull,
    backgroundColor: 'rgba(255,255,255,0.2)',
    marginTop: spacing.sm,
    overflow: 'hidden',
    flexDirection: 'row-reverse',
  },
  progressFill: {
    height: '100%',
    borderRadius: spacing.radiusFull,
    backgroundColor: colors.accentAction,
  },
  scroll: { flex: 1 },
  content: {
    width: '100%',
    maxWidth: 760,
    alignSelf: 'center',
    padding: spacing.lg,
    paddingBottom: spacing.xxl,
  },
  noyaRow: {
    flexDirection: 'row-reverse',
    alignItems: 'flex-start',
    gap: spacing.sm,
    marginBottom: spacing.xl,
  },
  noyaAvatar: {
    width: 56,
    height: 56,
    borderRadius: 28,
    borderWidth: 3,
    borderColor: colors.white,
    ...shadows.small,
  },
  noyaBubble: {
    flex: 1,
    backgroundColor: colors.white,
    borderRadius: spacing.radiusLarge,
    borderTopRightRadius: spacing.xs,
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.sm,
    ...shadows.small,
  },
  noyaClose: {
    position: 'absolute',
    left: spacing.sm,
    top: spacing.xs,
    width: 30,
    height: 30,
    alignItems: 'center',
    justifyContent: 'center',
  },
  noyaCloseText: { color: colors.textMuted, fontSize: 22, lineHeight: 24 },
  noyaName: {
    color: colors.primary,
    fontSize: 12,
    fontFamily: fontFamilies.semiBold,
    textAlign: 'right',
    writingDirection: 'rtl',
  },
  noyaMessage: {
    color: colors.textPrimary,
    fontSize: 15,
    lineHeight: 22,
    marginTop: spacing.xs,
    textAlign: 'right',
    writingDirection: 'rtl',
  },
  fieldLabel: {
    color: colors.textPrimary,
    fontSize: 14,
    fontFamily: fontFamilies.medium,
    marginBottom: spacing.sm,
    textAlign: 'right',
    writingDirection: 'rtl',
  },
  fieldHint: {
    color: colors.textMuted,
    fontSize: 12,
    lineHeight: 18,
    marginTop: spacing.sm,
    textAlign: 'right',
    writingDirection: 'rtl',
  },
  fieldError: {
    color: colors.error,
    fontSize: 12,
    fontFamily: fontFamilies.medium,
    marginTop: spacing.sm,
    textAlign: 'right',
    writingDirection: 'rtl',
  },
  modeActions: {
    flexDirection: 'row-reverse',
    gap: spacing.sm,
    marginTop: spacing.md,
  },
  modeButton: {
    flex: 1,
    minHeight: 48,
    borderWidth: 1,
    borderColor: colors.border,
    borderRadius: spacing.radiusMedium,
    backgroundColor: colors.white,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row-reverse',
    gap: spacing.xs,
    paddingHorizontal: spacing.sm,
  },
  modeButtonSelected: {
    borderColor: colors.primary,
    backgroundColor: colors.accentLight,
  },
  modeButtonText: {
    color: colors.primary,
    fontSize: 13,
    fontFamily: fontFamilies.medium,
    textAlign: 'center',
    writingDirection: 'rtl',
  },
  locationPanel: {
    marginTop: spacing.lg,
  },
  selectedDestination: {
    minHeight: 68,
    flexDirection: 'row-reverse',
    alignItems: 'center',
    gap: spacing.sm,
    padding: spacing.md,
    backgroundColor: colors.successLight,
    borderRadius: spacing.radiusMedium,
  },
  selectedDestinationIcon: {
    width: 34,
    height: 34,
    borderRadius: 17,
    backgroundColor: colors.white,
    alignItems: 'center',
    justifyContent: 'center',
  },
  selectedDestinationCopy: { flex: 1, alignItems: 'flex-end' },
  selectedDestinationTitle: {
    color: colors.textPrimary,
    fontFamily: fontFamilies.semiBold,
    fontSize: 15,
    textAlign: 'right',
  },
  selectedDestinationSubtitle: {
    color: colors.textSecondary,
    fontSize: 12,
    marginTop: spacing.xs,
    textAlign: 'right',
  },
  textAction: {
    color: colors.primary,
    fontFamily: fontFamilies.medium,
    fontSize: 13,
  },
  destinationPicker: { position: 'relative', zIndex: 10 },
  searchInputWrap: {
    minHeight: 50,
    flexDirection: 'row-reverse',
    alignItems: 'center',
    gap: spacing.sm,
    paddingHorizontal: spacing.md,
    borderWidth: 1,
    borderColor: colors.border,
    borderRadius: spacing.radiusMedium,
    backgroundColor: colors.white,
  },
  searchInput: {
    flex: 1,
    minHeight: 48,
    color: colors.textPrimary,
    fontSize: 16,
    writingDirection: 'rtl',
  },
  destinationResults: {
    marginTop: spacing.sm,
    backgroundColor: colors.white,
    borderWidth: 1,
    borderColor: colors.border,
    borderRadius: spacing.radiusMedium,
    overflow: 'hidden',
    ...shadows.small,
  },
  destinationResult: {
    minHeight: 58,
    flexDirection: 'row-reverse',
    alignItems: 'center',
    gap: spacing.sm,
    paddingHorizontal: spacing.md,
    borderBottomWidth: 1,
    borderBottomColor: colors.borderLight,
  },
  destinationResultCopy: { flex: 1, alignItems: 'flex-end' },
  destinationResultTitle: { color: colors.textPrimary, fontFamily: fontFamilies.medium },
  destinationResultSubtitle: { color: colors.textSecondary, fontSize: 12, marginTop: 2 },
  destinationEmpty: { color: colors.textSecondary, padding: spacing.md, textAlign: 'right' },
  manualMap: {
    width: '100%',
    height: 260,
    borderRadius: spacing.radiusLarge,
    overflow: 'hidden',
  },
  coordinateFields: {
    flexDirection: 'row-reverse',
    gap: spacing.sm,
    marginTop: spacing.md,
  },
  categoryGrid: {
    flexDirection: 'row-reverse',
    flexWrap: 'wrap',
    gap: spacing.sm,
  },
  categoryTile: {
    flexBasis: '47%',
    flexGrow: 1,
    minHeight: 76,
    borderWidth: 1,
    borderColor: colors.border,
    borderRadius: spacing.radiusMedium,
    backgroundColor: colors.white,
    alignItems: 'center',
    justifyContent: 'center',
    gap: spacing.xs,
    padding: spacing.sm,
  },
  categoryTileSelected: {
    borderColor: colors.primary,
    backgroundColor: colors.accentLight,
  },
  categoryTileText: {
    color: colors.textPrimary,
    fontSize: 13,
    fontFamily: fontFamilies.medium,
    textAlign: 'center',
    writingDirection: 'rtl',
  },
  categoryTileTextSelected: { color: colors.primary },
  taxonomyHeader: {
    flexDirection: 'row-reverse',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: spacing.md,
  },
  taxonomyTitle: {
    color: colors.textPrimary,
    fontSize: 16,
    fontFamily: fontFamilies.semiBold,
    textAlign: 'right',
  },
  suggestionPanel: {
    backgroundColor: colors.secondaryLight,
    borderRadius: spacing.radiusMedium,
    padding: spacing.md,
    marginBottom: spacing.md,
  },
  suggestionText: {
    color: colors.textPrimary,
    fontSize: 14,
    lineHeight: 20,
    textAlign: 'right',
    writingDirection: 'rtl',
  },
  suggestionActions: {
    flexDirection: 'row-reverse',
    gap: spacing.sm,
    marginTop: spacing.sm,
  },
  chipWrap: {
    flexDirection: 'row-reverse',
    flexWrap: 'wrap',
    gap: spacing.sm,
  },
  chip: {
    minHeight: 42,
    paddingHorizontal: spacing.md,
    borderWidth: 1,
    borderColor: colors.border,
    borderRadius: spacing.radiusFull,
    backgroundColor: colors.white,
    alignItems: 'center',
    justifyContent: 'center',
  },
  chipSelected: { borderColor: colors.primary, backgroundColor: colors.primary },
  chipText: { color: colors.textPrimary, fontSize: 13, fontFamily: fontFamilies.medium },
  chipTextSelected: { color: colors.white },
  moreButton: {
    minHeight: 44,
    alignSelf: 'flex-end',
    flexDirection: 'row-reverse',
    alignItems: 'center',
    gap: spacing.xs,
    paddingVertical: spacing.sm,
  },
  moreText: { color: colors.primary, fontFamily: fontFamilies.medium, fontSize: 13 },
  allSubcategories: { marginTop: spacing.md },
  fieldStack: { gap: spacing.md },
  photoHint: { marginTop: -spacing.sm, marginBottom: spacing.sm },
  preview: {
    backgroundColor: colors.white,
    borderRadius: spacing.radiusLarge,
    overflow: 'hidden',
    ...shadows.small,
  },
  previewImage: { width: '100%', height: 170, backgroundColor: colors.surfaceSubtle },
  previewPlaceholder: {
    width: '100%',
    height: 140,
    backgroundColor: colors.primary,
    alignItems: 'center',
    justifyContent: 'center',
  },
  previewCopy: { padding: spacing.md, alignItems: 'flex-end' },
  previewTitle: {
    color: colors.textPrimary,
    fontSize: 17,
    fontFamily: fontFamilies.semiBold,
    textAlign: 'right',
  },
  previewMeta: { color: colors.textSecondary, fontSize: 12, marginTop: spacing.xs, textAlign: 'right' },
  optionalTitle: {
    color: colors.textPrimary,
    fontSize: 15,
    fontFamily: fontFamilies.semiBold,
    marginTop: spacing.xl,
    marginBottom: spacing.sm,
    textAlign: 'right',
  },
  optionalField: { marginTop: spacing.md },
  footer: {
    backgroundColor: colors.white,
    borderTopWidth: 1,
    borderTopColor: colors.border,
    paddingHorizontal: spacing.lg,
    paddingTop: spacing.md,
    ...shadows.medium,
  },
  footerInner: {
    width: '100%',
    maxWidth: 760,
    alignSelf: 'center',
    flexDirection: 'row-reverse',
    alignItems: 'center',
    gap: spacing.sm,
  },
  primaryButton: {
    flex: 1,
    minHeight: 52,
    borderRadius: spacing.radiusMedium,
    backgroundColor: colors.primary,
    alignItems: 'center',
    justifyContent: 'center',
  },
  primaryButtonDisabled: { opacity: 0.5 },
  primaryButtonText: { color: colors.white, fontSize: 16, fontFamily: fontFamilies.medium },
  backButton: {
    minWidth: 60,
    minHeight: 48,
    alignItems: 'center',
    justifyContent: 'center',
  },
  backButtonText: { color: colors.primary, fontSize: 14, fontFamily: fontFamilies.medium },
});
