import React from 'react';
import DiscoveryActiveFiltersList from './DiscoveryActiveFiltersList';

export default function ActiveFiltersList(props) {
  return <DiscoveryActiveFiltersList {...props} />;
}
