rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Retired rollback bucket: no client access is permitted. Objects remain
    // available only to explicitly authorized operators during the hold.
    match /{path=**} {
      allow read, write: if false;
    }
  }
}
